# services restart removed

`openbase-coder services restart` is not a supported command.

## Usage

```bash
openbase-coder restart --service NAME
```

Use the top-level `restart` command for Openbase-managed services.

## Example

```bash
openbase-coder restart --service livekit-server
```
