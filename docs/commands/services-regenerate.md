# services regenerate

Recreate launchd wrappers and plists from current install config.

## Usage

```bash
openbase-coder services regenerate
```

After regenerating, reload with:

```bash
openbase-coder services install
```
